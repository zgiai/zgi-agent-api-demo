# ZGI local gateway. Keep the /api/v1 suffix.
ZGI_API_BASE_URL=http://localhost:2870/api/v1

# Create this key for a published Agent in the ZGI console.
# Never expose it through a NEXT_PUBLIC_ variable.
ZGI_AGENT_API_KEY=replace-with-your-agent-api-key
